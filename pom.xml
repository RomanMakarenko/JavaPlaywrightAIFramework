<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        JavaPlaywrightAIFramework — E2E UI test automation framework.
        Java 21 + TestNG + Playwright + Maven, Allure reporting, AI-assisted authoring.
        Spec & conventions: CLAUDE.md · Build plan: docs/IMPLEMENTATION_PLAN.md
    -->
    <groupId>com.funtime</groupId>
    <artifactId>JavaPlaywrightAIFramework</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>JavaPlaywrightAIFramework</name>
    <description>E2E UI test automation framework (Java + TestNG + Playwright + Maven + Allure)</description>

    <properties>
        <!-- Build -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <maven.surefire.version>3.5.6</maven.surefire.version>
        <exec.maven.version>3.6.3</exec.maven.version>
        <allure.maven.version>3.0.3</allure.maven.version>

        <!-- Dependencies (versions verified against Maven Central, 2026-08) -->
        <playwright.version>1.62.0</playwright.version>
        <testng.version>7.12.0</testng.version>
        <allure.version>2.35.4</allure.version>
        <aspectj.version>1.9.25</aspectj.version>
        <slf4j.version>2.0.18</slf4j.version>
        <logback.version>1.6.3</logback.version>
        <assertj.version>3.27.7</assertj.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Aligns all io.qameta.allure artifact versions (allure-testng, etc.) -->
            <dependency>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-bom</artifactId>
                <version>${allure.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Browser automation (used by main code: BaseTest/PlaywrightFactory) -->
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
        </dependency>

        <!-- Test runner -->
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
            Allure annotations for MAIN code (@Step on Page Object intent methods).
            Version from the BOM. Compile scope: Page Objects in src/main live outside the
            test classpath. Runtime step recording still comes from the AspectJ agent.
        -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-java-commons</artifactId>
        </dependency>

        <!-- Allure ↔ TestNG integration (version from the BOM) -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-testng</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            AspectJ weaver: runtime javaagent that auto-records Allure @Step calls.
            Attached via the surefire argLine below; version must match the jar path.
        -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>${aspectj.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Assertions (fluent assertThat(...)) -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Logging: facade + Logback implementation -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Java 21 for both main and test sources -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.version}</version>
                <configuration>
                    <!-- Single entry point for the suite: parallelism, groups, listeners (see Phase 6) -->
                    <suiteXmlFiles>
                        <suiteXmlFile>src/test/resources/testng.xml</suiteXmlFile>
                    </suiteXmlFiles>
                    <!-- AspectJ agent → automatic Allure step recording -->
                    <argLine>-javaagent:${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar</argLine>
                </configuration>
            </plugin>

            <!-- Generate the Allure report without a global CLI: mvn allure:report -->
            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>${allure.maven.version}</version>
            </plugin>

            <!--
                Install Playwright browsers (run once per machine / CI cache):
                mvn exec:java -e -Dexec.mainClass=com.microsoft.playwright.CLI -Dexec.args="install chromium"
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.maven.version}</version>
            </plugin>
        </plugins>
    </build>
</project>